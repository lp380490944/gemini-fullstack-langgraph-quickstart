# Core keys
GEMINI_API_KEY=
LANGSMITH_API_KEY=

# Optional basic auth for the /app frontend (via FastAPI middleware)
APP_BASIC_AUTH_USER=
APP_BASIC_AUTH_PASS=

# Optional: Proxy settings to reach external services (e.g., Google Gemini)
# Prefer socks5h so DNS also goes through the proxy
# Example: socks5h://user:pass@proxy-host:1080
ALL_PROXY=
HTTPS_PROXY=
HTTP_PROXY=
# Do not proxy internal service names and localhost
NO_PROXY=127.0.0.1,localhost,langgraph-redis,langgraph-postgres